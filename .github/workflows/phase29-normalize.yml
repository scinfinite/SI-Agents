name: Phase 29 Normalize
on:
  push:
    branches: [phase-29-agency-parity]
permissions:
  contents: write
jobs:
  normalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python tools/phase29_unique_names.py
