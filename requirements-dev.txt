# Deterministic development and CI tooling.
# Update these pins deliberately and verify the full suite before changing them.
build==1.3.0
pre-commit==4.3.0
pytest==8.4.2
ruff==0.13.1
setuptools==80.9.0
wheel==0.45.1
